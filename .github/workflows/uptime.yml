name: Keep Groovia Awake
on:
  schedule:
    - cron: "*/8 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep server awake
        run: |
          curl -L https://groovia-ewv0.onrender.com/health > /dev/null
